import { assertContractPanics, STORE_WORKSPACE } from "./test-utils";

STORE_WORKSPACE.test(
  "metadata",
  async (test, { root, contract, alice, bob }) => {
    // TODO::testing::medium: tests for metadata
    test.pass();
  }
);
